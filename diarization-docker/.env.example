NVIDIA_VISIBLE_DEVICES=0

# Hugging Face access token (never commit real values)
HF_TOKEN=

# Model identifiers (set your own values)
STT_MODEL=
GENDER_MODEL=
MSDD_MODEL=

# Internal STT endpoint used by diarization-service (compose default)
STT_API=http://stt-service:8000/transcribe/
